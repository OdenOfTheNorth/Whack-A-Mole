var GridSettings = pc.createScript('gridSettings');

GridSettings.attributes.add('maxTime', {
    type: 'number',
    default: 1
});

GridSettings.attributes.add('startRingSize', {
    type: 'number',
    default: 1
});

GridSettings.attributes.add('endRingSize', {
    type: 'number',
    default: 1
});